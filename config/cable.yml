development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:16381/1" } %>
  channel_prefix: chat_app_development

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: chat_app_production
